<p><%= flash[:error] unless flash[:error].blank? %></p>

<%= form_for([@user,@article,@comment,@comment.replys.build],:remote => true) do |f| %>
 	<div class="form-group">
    <div class="col-md-2">
    	<%= f.text_area :body,class:"form-control" %>
  	</div>
  </div>	
  
 	<div class="form-group">
    <div class="col-md-2">
    	<%= f.submit  %>
  	</div>
  </div>
<% end %>

