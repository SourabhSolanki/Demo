<p><%= flash[:sucess] unless flash[:sucess].blank? %></p>
<p>
  <strong>Title:</strong>
  <%= @article.title %>
</p>
<p>
  <strong>Text:</strong>
  <%= @article.text %>
</p>

<h2>Comments</h2>
<div id='com'>
  <%= render 'comments/comment', comments: @article.comments %>
</div>
<h2>Add a comment:</h2>
<%= render 'comments/form' %>

<%= link_to 'Back',  if @user.user? 
                         user_home_user_path(params[:user_id])
                    else
                         admin_home_user_path(params[:user_id])
                    end %>